{
  "name": "jest-bin",
  "version": "0.2.3",
  "description": "Find and run the appropriate jest bin file among the installed react-app-rewried, react-scripts and jest",
  "author": "SSen",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write \"**/*.{js,ts,tsx}\""
  },
  "bin": {
    "jest-bin": "bin/jest-bin.js"
  },
  "dependencies": {
    "react-dev-utils": "^11.0.0"
  },
  "devDependencies": {
    "@ssen/prettier-config": "^1.2.0",
    "prettier": "^2.2.1"
  }
}
